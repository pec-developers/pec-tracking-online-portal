## build-validation

Rules:
- After making any changes to the frontend or backend codebase, you MUST always run the production build command (`npm run build` or equivalent compile-time verification commands) to ensure there are no compilation, type-checking, or bundling issues before ending your turn.
